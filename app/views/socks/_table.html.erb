<table class="table table-light table-striped">
  <thead>
    <tr>
      <th>Code name</th>
      <th>First name</th>
      <th>Last name</th>
      <th>Location</th>
    </tr>
  </thead>

  <tbody>
    <% @socks.each do |sock| %>
      <tr>
        <td><%= link_to sock.code_name, sock %></td>
        <td><%= sock.first_name %></td>
        <td><%= sock.last_name %></td>
        <td><%= sock.city_and_state %></td>
      </tr>
    <% end %>
  </tbody>
</table>
